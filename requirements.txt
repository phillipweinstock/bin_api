# Core API dependencies
fastapi
uvicorn[standard]
pydantic
python-multipart
requests

# Hardware libraries (Mandatory for actual hardware, mocking for development only)
# These will fail gracefully on non-Raspberry Pi systems
RPi.GPIO; platform_machine == "aarch64" or platform_machine == "armv7l"
adafruit-circuitpython-servokit; platform_machine == "aarch64" or platform_machine == "armv7l"
gpiozero; platform_machine == "aarch64" or platform_machine == "armv7l"
git+https://github.com/pololu/motoron-python.git; platform_machine == "aarch64" or platform_machine == "armv7l"
lgpio; platform_machine == "aarch64" or platform_machine == "armv7l"
adafruit-circuitpython-dht; platform_machine == "aarch64" or platform_machine == "armv7l"
spidev; platform_machine == "aarch64" or platform_machine == "armv7l"
dbus_next; platform_machine == "aarch64 or platform_machine == "armv7l"